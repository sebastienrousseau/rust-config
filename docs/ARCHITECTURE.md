# `@sebastienrousseau/rust-config` Architecture

Shareable Rust configuration providing standardized rustfmt and clippy rules adhering to modern 2026 standards.

## Design Principles
- Single source of truth for Rust fleet configurations.
- Multi-format exports (CommonJS, ESM, TypeScript declarations).
- 100% test coverage with zero runtime production dependencies.
